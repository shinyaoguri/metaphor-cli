# {{PROJECT_NAME}}

A metaphor sketch generated with:

```bash
metaphor new {{PROJECT_NAME}} --template {{TEMPLATE_ID}}
```

## Run

```bash
swift run
```

The sketch entry point is `Sources/{{MODULE_NAME}}/App.swift`.

For AI-assisted iteration, start with `AGENTS.md` and keep the creative target in `PROJECT_BRIEF.md`.
